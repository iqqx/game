export class Weapon {
    static WeaponsPath = "Assets/Weapons/";
    static Weapons = [];
    static Load() {
        console.log();
        // readdir(Weapon.WeaponsPath, undefined, (x, f) => {
        // 	console.log(f);
        // });
        // fetch(Weapon.WeaponsPath + folderName + "/main.json")
        // 	.then((x) => x.json())
        // 	.then((x) => console.log(x));
    }
}
